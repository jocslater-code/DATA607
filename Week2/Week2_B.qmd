---
title: "Week 2A Assignment: Approach"
author: "Jocelyn Slater"
format: html
editor: visual
---

```{r load-packages, message=FALSE, echo=FALSE}
library(tidyverse)
library(readr)
library(data.table)
library(ggplot2)
```

## Task

Analyze the performance of a binary classification model and develop intuition for how probability thresholds affect model evaluation metrics.

## Outline

- Setup
  - Download files from the course GitHub repo
    - [`penguin_predictions.csv`](https://raw.githubusercontent.com/acatlin/data/refs/heads/master/penguin_predictions.csv)
    -   **`.pred_female`** – Model-predicted probability that the observation belongs to the “female” class
    -   **`.pred_class`** – Predicted class label (1 if `.pred_female > 0.5`, otherwise 0)
    -   **`sex`** – Actual class label used during model training

- Analysis

  - Null Error Rate
    - Inspect class balance (`count(sex)`).
    - Calculate the null error rate (majority-class error rate).
    - Plot the actual class distribution of the target variable using `ggplot2`. 
    - Explain why knowing the null error rate is important when evaluating models.
    - Recompute predicted class (ifelse(.pred_female \> threshold, 1, 0))
  -   Build Confusion Matrices
      -   Using probability thresholds of 0.2, 0.5, and 0.8, compute:
          -   True Positives (TP)
          -   False Positives (FP)
          -   True Negatives (TN)
          -   False Negatives (FN)
      -   Present results as three confusion matrices
      -   Create logical conditions for TP, FP, TN, FN - count rows for each condition
  -   Derive Metrics
      -   For each threshold (0.2, 0.5, and 0.8), calculate:
          -   Accuracy = (TP + TN) / total
          -   Precision = TP / (TP + FP)
          -   Recall = TP / (TP + FN)
          -   F1 = harmonic mean of precision and recall
      -   Present results in a clear table.
  - Threshold Use Cases
      -   Describe at least one real-world scenario where:
          -   A **0.2 threshold** is preferable
          -   A **0.8 threshold** is preferable
       
          
## Anticipated Challenges
- Apostrophes in last names 
  - For now I'll just drop them
- Never used SQL before, learning curve interfacing with new software
- 


## Submission Requirements

-   Post all code (SQL, R, Quarto) in a **GitHub repository**

-   Submit the GitHub link

-   RPubs publication is *not required*

-   Group work is allowed, but each student must submit individually and list collaborators         
